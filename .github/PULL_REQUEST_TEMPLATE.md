## Summary

<!-- 1-3 sentences on what changed and why. Link any related issue with `Closes #123`. -->

## Type of change

<!-- Match the Conventional Commit prefix of your commit message. release-please uses it for the changelog. -->

- [ ] `feat:` — new user-facing feature
- [ ] `fix:` — user-visible bug fix
- [ ] `perf:` — performance improvement
- [ ] `refactor:` — internal change with no behaviour delta
- [ ] `docs:` — documentation only
- [ ] `test:` — test-only change
- [ ] `chore:` / `build:` / `ci:` — tooling, deps, pipeline

## Test plan

- [ ] `yarn eslint` passes locally.
- [ ] `yarn tsc-test` passes locally.
- [ ] `yarn vitest` passes locally with 100% coverage on changed files.
- [ ] `yarn build` succeeds.

## Notes for reviewers

<!-- Anything non-obvious about the diff, trade-offs you considered, follow-ups you deferred. -->
